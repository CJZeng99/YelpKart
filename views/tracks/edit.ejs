<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Track</title>
</head>

<body>
    <form action="/tracks/<%= track._id %>?_method=PUT" method="POST">
        <div>
            <label for="name">Name</label>
            <input type="text" id="name" name="track[name]" value="<%= track.name %>">
        </div>
        <div>
            <label for="location">Location</label>
            <input type="text" id="location" name="track[location]" value="<%= track.location %>">
        </div>
        <button>Edit Track</button>
    </form>
    <a href="/tracks/<%= track._id %>">Back To Track</a>
</body>

</html>